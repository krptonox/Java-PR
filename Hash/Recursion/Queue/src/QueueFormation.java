import java.util.*;
public class QueueFormation {
    static void main() {
     Queue<Integer> q = new LinkedList<>();
        q.add(10);
        q.add(11);
        q.add(12);
        q.add(13);
        System.out.println(q.remove());
        System.out.println(q.remove());

        Deque<Integer> dek = new ArrayDeque<>();

    }
}
